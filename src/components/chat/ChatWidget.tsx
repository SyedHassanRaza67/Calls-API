// The AI help chat has been removed in the self-hosted build.
// This is a no-op stub kept so existing imports don't break. It renders nothing
// and makes ZERO network calls.
export default function ChatWidget() {
  return null;
}
